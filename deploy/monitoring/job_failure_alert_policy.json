{
  "displayName": "Cloud Run Job failure — backtest-screen / backtest-sweep / data-backfill",
  "combiner": "OR",
  "conditions": [
    {
      "displayName": "A Cloud Run Job logged an error",
      "conditionMatchedLog": {
        "filter": "resource.type=\"cloud_run_job\" AND resource.labels.job_name=(\"backtest-screen\" OR \"backtest-sweep\" OR \"data-backfill\") AND severity>=ERROR"
      }
    }
  ],
  "documentation": {
    "content": "A Cloud Run **Job** logged an error. Until FC-096 Phase A nothing in this project watched Jobs: every other policy here matches `cloud_run_revision` (the three services) or `build`, so a Job that failed, or quietly stopped being executed, was invisible.\n\nWhy that matters more for Jobs than for services: **Cloud Scheduler's `jobs:run` is asynchronous.** It records success when the API call returns, not when the execution finishes. A scheduler firing at a Job that fails every time, or at a Job that no longer exists, looks perfectly healthy in the scheduler's own history. This policy is one of two controls that see through that; the other is `/regression`'s `lake_freshness` check, which catches the case where the scheduler is paused or deleted and so never fires at all.\n\nThe three Jobs it watches:\n\n- **`backtest-screen`** — the monthly universe screen (`monthly-performance-review`, 1st at 06:00 ET). Its failure means no `backtest_runs` row for the month.\n- **`backtest-sweep`** — dashboard-submitted scenario sweeps. A failure is usually visible to the submitter as a `failed` row in `scenario_sweeps`; the exception is a container killed before it could write one, which is exactly what this catches.\n- **`data-backfill`** — the weekly chain/bars backfill (`backfill-weekly`, Saturdays 08:00 ET). Its failure is the quiet one: nothing downstream breaks immediately, the lake just stops getting fresher, and the first symptom would be a sweep silently materialising cold months later.\n\n**One condition, deliberately.** A log-based alert policy takes exactly one `conditionMatchedLog`; every one of the live policies in this project is single-condition for that reason. The filter is `severity>=ERROR`, which catches the Jobs' own structured failure events — `backfill_symbol_failed`, `backfill_day_failed`, `sweep_materialise_failed`, `sweep_store_unavailable` — since `main.py` logs them through structlog at ERROR with JSON payloads. The platform's own task-failure record is an audit entry (`protoPayload`), not a `textPayload` line, so it is not what this matches; the Job's own error output is, and every failure path in `main.py` produces one before the process exits.\n\nTriage:\n\n```\ngcloud run jobs executions list --job=data-backfill --region=us-central1 --limit=5\ngcloud run jobs executions describe <EXECUTION> --region=us-central1\ngcloud logging read 'resource.type=\"cloud_run_job\" AND labels.\"run.googleapis.com/execution_name\"=\"<EXECUTION>\"' --limit=100\n```\n\nFor `data-backfill` specifically, the per-symbol `backfill_symbol_complete` log lines are the record of what a half-finished run actually wrote — a killed execution leaves no terminal summary — and the Job is idempotent, so the remedy is almost always \"execute it again with the same window\".\n\nNote the exit-code contract: `--command backfill` exits non-zero when any symbol hit a genuine **failure** (a vendor error, an exception mid-build, a symbol whose bars never arrived, an unusable close) or when a configured chain lake did not take everything it was given. A day skipped for an unmodelled **corporate action** is deliberately NOT one of those: it is counted and logged (`backfill_day_skipped` with `reason=corporate_action`, and `days_skipped_corporate_action` in the summary) but never reaches the exit code. A stock split recurs across the universe and the trailing window slides, so paging every Saturday for the month it takes to age out — on a condition nobody can act on — is how this policy would get muted, and a muted policy is not watching the failures that matter either. If you want to know which days the lake is missing, read the skip events; do not expect an alert for them.",
    "mimeType": "text/markdown"
  },
  "notificationChannels": [
    "projects/gen-lang-client-0607444019/notificationChannels/10474915111056992031"
  ],
  "enabled": true,
  "alertStrategy": {
    "notificationRateLimit": {
      "period": "300s"
    },
    "autoClose": "86400s"
  }
}
