# Plan: FC-096 Phase B — the interactive sim service, detail artifacts, engine-identity dedup, pinning + weekly battery

**FC entry:** `docs/FUTURE_CONSIDERATIONS.md` FC-096 (implements signed decision D1)
**Plan file:** `docs/plans/fc-096-b.md`
**Status:** Executing — PR-a **merged** (#109, `fed6cec`); PR-b open (#112); PR-c/d not started. Plan approved at rev 3 (two reviews REQUEST_CHANGES → rev 2 → confirmation FIXES-INCOMPLETE on one NEW blocker (CPU throttling under request billing) + four text items → rev 3 applies the prescribed fixes)
**Scope:** shared (backtesting engine, new Cloud Run service, dashboard backend, `cloudbuild.yaml`, BigQuery additive, GCS artifacts)
**Size:** XL (three PRs)
**Author:** Claude (Fable), under the operator's standing authorization (2026-09-01)
**Last updated:** 2026-09-01 (rev 3; PR-a Execution backfilled)

---

## Context

Phase A gave the data (22-reach lake, candidates, weekly refresh, DTE knob); the
operator experience is still batch-shaped (~3 min Job cold start per submit) and
the replay's rich detail (`DailyState` curve, `LedgerEvent` ledger,
`WheelCycle`s, roll records) is discarded after aggregation — Phase E's console
cannot be built on scalars. Signed decision D1: scale-to-zero sim service,
results persist forever, dedup re-keyed off `git_commit`, pinned combos
re-measured weekly.

**Round-1 verdicts (2026-09-01): both REQUEST_CHANGES, and they changed the
architecture.** The measured facts that killed rev 1's sync-response design:
warm-lake materialise costs 25–50 s per symbol (it dominates; replay is ~0.3
s/symbol-year), and THIS image running as a service shows a **240 s+ cold-start
tail** (14 days of live probe data: 10 startup-probe DEADLINE_EXCEEDED failures,
503s at 241–246 s, successful cold hits 23–52 s). A 300 s request budget bounded
by cell count bounds the wrong variable, and timeout-then-scale-down re-creates
the uncooperative-termination hole Phase A closed on the Job. Rev 2 is
**async-first**: the service persists and returns a handle; the console polls
the status machinery FC-060 already shipped.

## Goals

1. **`sim-service`**: private, scale-to-zero Cloud Run service. `POST /simulate`
   validates, dedups, and returns fast; results arrive via the existing
   status/results reads. Warm end-to-end for a small spec: seconds. Cold first
   hit: honest (the UI shows "warming"; measured 23 s–4 min tail), and warmth
   stays a one-flag upgrade per D1.
2. **Detail artifacts**: every replay cell persists curve/ledger/cycles/rolls to
   GCS with a pinned schema; dashboard can stream them (rendering is Phase E).
3. **Engine-identity dedup**: stored results survive deploys that cannot change
   replay output; anything that CAN change output invalidates.
4. **Pinning + weekly battery** with per-pin isolation and non-paging failure
   posture.

## Non-goals

- No covered-call replay (C), no IAP/roles (D — submit keeps the bearer token;
  the artifact endpoint's public-dashboard exposure question is explicitly
  punted to Phase D/FC-094 scope), no console UI (E). `/sims` is untouched.
- No new image (bot image, different command). A dedicated slim image only if
  rollout §2's measured cold p95 exceeds 120 s.
- No `min-instances` by default (D1); scheduled market-hours warmth stays a
  documented one-flag option (~$4–8/mo) the operator can turn on if the cold
  tail annoys in practice.
- The `backtest-sweep` Job path is unchanged and remains the big-batch route.

## Three PRs (review-required split; each independently rollback-able)

- **PR-a — engine identity + FC-092 fix**: the dedup re-key (the riskiest data
  contract), identity columns, and the tally fix the warm service needs anyway.
- **PR-b — detail artifacts**: engine serialisers + Job/CLI writers + dashboard
  streaming endpoint (+ its GCS dependency + bucket grant).
- **PR-c — the service**: FastAPI app, proxy, 4th deploy chain, coverage guard.
- PR-2 of rev 1 (pinning + battery) becomes **PR-d**, unchanged in scope.

## Proposed approach

### B1 — engine identity (PR-a)

- `src/backtesting/scenarios/engine_identity.py`: `engine_identity()` = 16-hex
  sha256 over **sorted relative paths + file contents of `src/**` — ALL file
  types** (the replay executes live strategy code: `src/strategy/**`,
  `src/api/**`, `src/data/**`, `src/utils/**`; and the two committed data
  tables `src/backtesting/data/{earnings_dates,dividend_history}.json` are
  behaviour-bearing replay inputs). `ENGINE_VERSION` mixed in. Cached per
  process; content-only (mtime/`__pycache__`-immune — `.pyc` is not `*.py` and
  site-packages is outside the tree). Docs/dashboard/deploy/cloudbuild churn
  does not move it; a one-byte `put_seller.py` change does. **Required tests:
  sensitive to a `src/strategy/**` byte AND to a byte of either JSON table;
  insensitive to `docs/**`, `dashboard/**`, `deploy/**`, `cloudbuild.yaml`.**
- `identity.sweep_key`: `git_commit` component → `engine_identity`;
  `git_commit` stays stored (provenance). One-time cache invalidation accepted
  on the record (first re-submit of every old spec recomputes once; the first
  battery Saturday after ANY engine change is the expensive one — operator
  note). FC-095's dedup bullet is executed by this PR; strike at closeout.
- **Dashboard source of the value (rev-1 blocker):** the dashboard image cannot
  hash a tree it does not ship. `cloudbuild.yaml`'s dashboard build computes
  the hash from the same checkout (one `python - <<… engine_identity …` step
  writing a build-arg) and bakes it as `ENGINE_IDENTITY` env into the image;
  `services/sweeps.py` reads the env for its `sweep_key` hint and stamps it on
  `submitted` rows. **Parity contract test**: repo-side test computes
  `engine_identity()` and asserts the cloudbuild step's derivation script is
  byte-identical logic (shared module invoked, not reimplemented); rollout
  verifies live `/health` (service) == dashboard env. If the env is absent the
  hint path DISABLES itself loudly (log event) rather than keying wrong.
- Both sweep tables gain additive NULLABLE `engine_identity`; both dedup
  predicates require equality on it.
- **FC-092 fix (folded in, closes that FC's item 1):** `setup_logging`'s
  `cache_logger_on_first_use=True` makes every strategy logger bind the
  process's FIRST `RejectionTally` forever (measured: second in-process run
  gets `{}`). Fix at the mechanism: the tally binds through a `contextvars`/
  proxy-stable indirection instead of relying on `structlog.configure` in
  `__enter__` (exact idiom builder's choice; the CONTRACT is: N sequential
  `run_sweep` calls in one process each get their own complete tally —
  regression test with two runs asserting equal non-empty tallies). Item 2
  (ordering nondeterminism) fixed by sorting the summary; `PYTHONHASHSEED`
  no longer load-bearing for artifacts.

### B2 — detail artifacts (PR-b)

- `src/backtesting/reporting/artifact.py`: `cell_artifact(result, meta) ->
  dict` serialising `daily` (INCLUDING `shares_held`), full `LedgerEvent`s,
  `build_cycles` rows, `roll_records`, dividend/early-assignment counters,
  rejection tally (post-FC-092 fix), and provenance: `run_id`, scenario,
  symbol, split, `scenario_hash`, `config_hash`, `engine_identity`,
  **the arm's MASKED reach** (per fc-096-a per-arm masking — never the parent
  `materialised.max_dte`), window, and the **fill assumption of the serialised
  replay** (`mid`, `fill_haircut`) — the row's `bid_fill_return` scalar comes
  from a bid replay that deliberately gets no artifact, and the stamp is what
  keeps the pair honest. In-JSON `"schema": 1`; **frozen-fixture test pins the
  full key set including per-`kind` `LedgerEvent.detail` keys**; Phase E's
  component inventory (FC-096 entry) is the required-fields list.
- `_replay_one` gains `artifact_sink=None`; called LAST, after the row is
  built, in its own try (a sink raise must not error the row; a row error means
  no sink call — `artifacts_complete` ≡ artifact count == non-error cell
  count, stored as a flag on the done row). CLI writes artifacts only under
  `--persist` (Layer-2 contract pinned by test); Job writes always.
- Storage: `gs://gen-lang-client-0607444019-options-data/sim-artifacts/v1/
  <run_id>/<scenario>__<symbol>__<split>.json.gz`. Parse only by
  `rsplit("__", 2)`; `validate_scenario_name` gains a "no `__`" rule (shared
  constant, engine + dashboard). Verified: bucket has no lifecycle/retention
  rules. **Rollout adds the explicit IAM grant** (`objectAdmin` for the compute
  SA on this bucket, as the lake has) — today's access rides the legacy
  project-Editor convenience binding and would break silently under an Editor
  cleanup.
- Dashboard: `GET /api/v2/sweeps/{run_id}/artifacts/{scenario}/{symbol}/{split}`
  streams the object. **`google-cloud-storage` added to
  `dashboard/backend/requirements.txt`** (new dependency, named).

### B3 — the service (PR-c)

- `deploy/sim_service.py`, FastAPI, **sync `def` handlers** (an `async`
  **CPU allocation (rev-3 blocker fix): `--no-cpu-throttling` — instance-based billing.** Under default request-based billing the 202-then-background-replay design starves: CPU throttles once the request returns, and the poll never touches this service (it reads BigQuery via the dashboard). Instance-based billing is the documented mechanism for post-response work: ~$0.09/hr at 2 Gi / 1 vCPU only while an instance is ALIVE, $0 when fully idle — D1's substance ($0 idle, seconds warm) holds; its 'pay only per request' phrasing is corrected on the record. Pinned in deploy flags AND the cloudbuild contract test. Residual, accepted and documented: an idle instance can scale in mid-replay; SIGTERM terminalises the run as `failed` and the console's posture is resubmit — rare, bounded by one spec.
  handler would starve `/health` for the whole replay), `--concurrency=1`
  pinned in deploy flags AND the cloudbuild contract test (the engine's
  process-global swaps — `_failed_symbols`, analytics singleton, log levels —
  are safe only serialized; module docstring states the invariant).
  Resources: **2 Gi / 1 vCPU** (tmpfs bar/chain caches count against memory;
  512 Mi would OOM), `--timeout=120` (requests are now short), max-instances 2,
  min-instances 0.
- `POST /simulate` — same spec schema as the Job. Flow: validate (engine
  validators; the service holds a real `Config`) → coverage guard (below) →
  dedup by engine-identity key (`force` honoured) — **hit: return `200 {run_id, deduplicated: true}` — no rows; the console GETs
  the run via the dashboard, which shapes (consistent with §Who shapes)** → miss: write `submitted` +
  `running` rows (`submitted_via='sim-service'`, and the row carries
  `liveness_seconds=900` — see below) → **spawn the replay on the instance's
  single worker thread and return 202 + `run_id` immediately**; the console
  polls the existing `GET /api/v2/sweeps/{run_id}`. Persist rows + artifacts
  as cells complete; terminal row in a `finally` **with the Job's SIGTERM
  discipline copied** (handler armed, SIG_IGN around final writes) — rev 1
  waived this and the review was right that scale-down-after-timeout is
  routine, not exotic.
- **Liveness (rev-1 HIGH):** `blocking_sweep` and `is_stuck` read a new
  optional `liveness_seconds` off the row (NULL → today's Job clock), so a
  killed service instance releases the submit lock in ~25 min (900 s liveness + the existing 10-min grace), not
  3 h 10 m. Test both writers' rows against both readers.
- **Budget honesty:** the pre-flight estimates `symbols × splits ×
  MATERIALISE_SECONDS_PER_SYMBOL` (constant from rollout §2's measurement,
  asserted against it in a test) + replay seconds; a spec estimated over
  `SIM_MAX_ESTIMATE_SECONDS = 600` is refused with "use the batch path"
  (numbers in the 409 body). `run_sensitivity` counts double or is refused on
  the service path (builder: refuse; the flip flag is for the arm you finally
  choose — CLI/Job territory). Cell cap stays as a secondary bound.
- **Coverage guard, both halves (rev-1 blocker):** (a) pre-flight = one
  `list_blobs(prefix=chains/v1/<SYM>/)` per symbol (measured 0.55 s), dates
  intersected with the session calendar over the window — mid-window holes
  (corporate-action skip days are permanent-by-design) and boundary
  weekends both handled; incomplete → 409 naming symbol-days + the backfill
  remedy; (b) engine-side: the service materialises behind a
  **fetch-refusing provider wrapper** — any vendor call during materialise
  fails the run loudly (`sim_service_cold_fetch_refused`). The no-vendor
  contract is enforced, not assumed. Bars: the pre-flight does NOT probe bars
  (vendor bars are permitted — they are cheap and rate-benign relative to
  chains; stated explicitly, with the FC-095 data-key item cross-referenced).
- **Auth (rev-1 HIGH):** the proxy uses the **OIDC identity-token pattern of
  `dashboard/backend/routers/live.py`** (audience = service URL) — NOT
  `_launch_job`'s OAuth access token; the plan names the file because the two
  are easy to confuse and the repo's own docstring says so. Proxy
  `POST /api/v2/sims/run` keeps `SWEEP_SUBMIT_TOKEN` until Phase D. Service
  private: invoker = compute SA only. `@require_account_match` deliberately
  absent (no trading).
- `GET /health` returns `engine_identity` (smoke + parity verification).
- **4th deploy chain, light smoke (rev-1 HIGH):** `deploy-sim-canary` →
  `smoke-test-sim` = `/health` (identity present) + one `POST /simulate` of a
  spec engineered to be a **dedup hit** (the smoke accepts {200 dedup-hit | 202 accepted}, so the FIRST deploy and a fresh project pass on the 202; the strict dedup-hit assertion moves to rollout verification after §3 seeds the spec) — no
  replay, no BQ-write dependency beyond one read; the real cold simulate is
  rollout verification, not a deploy gate. Timing budget REDONE on the
  record: measured single-build max 827 s + light 4th chain (~+90–150 s
  deploy+smoke+promote) → ~975 s single, ~1335 s waiting-pair worst vs the
  1500 s timeout (~165 s headroom). `timeout:` stays 1500 s; if the measured
  post-merge max exceeds 1000 s, raise to 1800 s + `BUILD_TIMEOUT_SECONDS` in
  one commit (contract test pins them equal). Smoke rows: `submitted_via`
  is `'smoke'` for the seeded spec so battery/trend queries exclude it.
- **Who shapes:** the service returns raw rows/status only; the DASHBOARD
  shapes (its `shape_results` already does) — the shaper is not duplicated
  into the engine image (rev-1 MEDIUM stated ambiguity, resolved).

### B4 — pinning + weekly battery (PR-d)

As rev 1, plus the review's postures: pins are **revalidated at battery time**
(allowlist rules move — Phase A PR-2 proved it); an invalid/failing pin gets its
own `failed` row + loud event and the loop continues; a pin invalid 3
consecutive weeks emits a nag event. **Exit-code classes separated:** data
(backfill) failure pages via the Job policy; battery failure exits 0 with a
`battery_degraded` ERROR log watched by a 24 h-nag policy (trend staleness is
not a page — the cry-wolf principle). Battery wall-time capped
(`BATTERY_MAX_SECONDS`, refuses new sweeps past it, finishes the current one);
sized on the record: worst case (engine-change week: ~14 base + ≤20 pins,
lake-warm materialise dominated) must fit the shared 21600 s with the backfill;
the battery runs only after backfill success. `submitted_via='battery'`
(plumbing verified free-form; schema comment updated).

## Critical files

PR-a: `engine_identity.py` (new) · `identity.py` · `persist.py` +
`services/sweeps.py` (columns, predicates, env read) · `src/utils/logger.py` +
tally binding (FC-092) · `cloudbuild.yaml` (dashboard build-arg step) · docs.
PR-b: `reporting/artifact.py` (new) · `runner.py` (`artifact_sink`) ·
`main.py` (persist wiring) · dashboard router/service + requirements · docs.
PR-c: `deploy/sim_service.py` (new) · `persist.py` (`liveness_seconds` additive column — schema owner; reader in `services/sweeps.py`) · provider fetch-guard wrapper ·
`routers/v2.py` proxy · `cloudbuild.yaml` 4th chain + contract fixture ·
`services/sweeps.py` (`liveness_seconds` reader) · docs.
PR-d: `persist.py` (pins) · dashboard pins endpoints · `main.py`
(`--command battery`) · backfill Job wrapper · monitoring (battery nag policy).

## Reuse

`run_sweep`/status rows/dedup/`shape_results` wholesale; `evaluate.py` cycle/
fitness builders; `routers/live.py` OIDC pattern; sweep-Job SIGTERM discipline;
`OpportunityStore` GCS idioms; FC-060 polling UI unchanged.

## Behaviour contract

- Dedup hit (service or Job, same engine identity): answer < 2 s, zero replays.
- Deploys not touching `src/**` leave every stored key valid (test).
- A killed service instance releases the submit lock in ~25 min.
- The service can never reach the options vendor: guard test with a
  hole-in-the-middle window asserts refusal, not silent fetch.
- Artifact absence never breaks results; `artifacts_complete` accounts
  non-error cells exactly.
- CLI without `--persist` writes nothing anywhere.
- N sequential sweeps in one process each get complete, sorted tallies.

## Test requirements

Identity: sensitivity/insensitivity set incl. `src/strategy` byte and JSON
tables; dashboard parity (shared derivation, env read, absent-env loud
disable); key migration. FC-092: two-runs-one-process tally equality; sorted
summary. Artifacts: frozen-key fixture (schema 1, detail kinds), builder
equality with `evaluate.py`, masked-reach stamp, sink ordering/error cases,
Layer-2. Service: estimate-refusal boundaries; sensitivity refusal; coverage
409 naming days; fetch-guard; 202 flow writes rows then completes; SIGTERM
terminalises; liveness release both readers; concurrency/contract flags;
smoke-marker exclusion. Battery: per-pin isolation, revalidation, nag, exit
classes, wall cap. Cloudbuild: 4th chain pinned, budget numbers in the
fixture comment.

## Open questions

- **Non-blocking:** artifact retention (none; revisit at 1 GB).
- **Non-blocking:** scheduled market-hours warmth — operator flag, documented,
  off by default (D1).
- **Non-blocking (Phase D):** artifact endpoint sits on the public dashboard
  until IAP lands — same exposure class as FC-094, called out there.

## Rollout

0. **Pre-deploy migration (DONE 2026-09-01 by the agent, on the record):** additive `ALTER TABLE options_wheel.{scenario_sweeps,scenario_runs} ADD COLUMN engine_identity STRING` — the identity/dedup review proved the first post-merge deploy would otherwise 500 every dashboard submit (only the Job reconciles schema; the reconcile is unreachable through the broken path). The dashboard additionally degrades gracefully on an unrecognized column (hint-miss + insert-retry-without-field) as belt-and-braces — but that degraded state is not the intended one: it disables the dedup hint, and logs `sweep_dedup_hint_disabled` / `engine_identity_column_missing` once per process, until the column exists.
1. Merge PR-a. Verify: docs-only push → stored runs still dedup-hit;
   `src/strategy` touch (next real PR) → recompute; dashboard env parity.
2. Merge PR-b. Verify artifact objects + streaming endpoint on a CLI
   `--persist` sweep; bucket IAM grant (operator or agent-SA if permitted).
3. Merge PR-c. **Measure on the record:** cold p50/p95 first-hit, warm
   materialise s/symbol (sets `MATERIALISE_SECONDS_PER_SYMBOL`), dedup-hit
   latency. Seed the smoke spec. If cold p95 > 120 s, open the slim-image
   follow-up; if build max > 1000 s, the 1800 s timeout commit.
4. Merge PR-d. Saturday: backfill-then-battery; verify exit classes and pin
   isolation on a deliberately-invalid pin.
5. FC-096 entry: Phase B shipped; FC-092 closed (item 1+2); FC-095 dedup
   bullet struck; Phase C plan next.

## Review disposition (round 1 → rev 2)

Engine/data reviewer: B1 boundary → `src/**` all-files (+tests named); B2
JSONs → included; B3 dashboard source → baked env + parity + loud disable;
H4 liveness → `liveness_seconds`; H5 FC-092 → fixed in PR-a; H6 probe →
list_blobs-intersect + fetch-refusing provider; H7 budget → estimate-based
refusal, sensitivity refused, measured constant; M8 → sync handlers +
concurrency 1 pinned; M9 artifact honesty → fill stamp, masked reach,
non-error accounting, sink ordering, schema pin, `shares_held`; M10/M11
battery → revalidation, isolation, exit classes, wall cap, sizing on record;
L12 → API-normalised spec keys; L13 → `/health` identity; L14 → exempt the
service logger from `_QUIET_LOGGERS`; L15 → `__` forbidden in names.
Svc/infra reviewer: BLOCKER-1/2 → async-first 202 design + SIGTERM discipline
+ honest cold-start posture + measurement gates; BLOCKER-3 → baked env (as
above); BLOCKER-4 → guard both halves; HIGH-1 → `routers/live.py` OIDC named;
HIGH-2 → JSONs in hash; HIGH-3 → light smoke + budget redone + escalation
rule; MEDIUM-1 → requirements dep named; MEDIUM-2 → bucket grant in rollout;
MEDIUM-3 → battery exit classes + cap; MEDIUM-4 → 2Gi/1cpu; MEDIUM-5 →
dashboard shapes; MEDIUM-6 → `submitted_via='smoke'`; LOW-1 → `__` rule;
LOW-2 → operator note on the expensive Saturday. Both critiques' sync-vs-202
argument: adopted (the rejected-alternative paragraph rev 1 owed is this Confirmation (rev 2 → rev 3): `--no-cpu-throttling` instance billing chosen (the confirmer proved the poll never supplies an in-flight request, so the background replay would starve on throttled CPU); dedup-hit returns ids not rows; smoke accepts {200|202} pre-seed; lock bound ~25 min; `liveness_seconds` schema ownership → PR-c. Code-review round (PR-a, #109): logging review APPROVE (fix held under threads/asyncio/exceptions/seeds; thread-contract tests required by PR-c); identity review REQUEST_CHANGES — live-schema window (→ rollout step 0 + dashboard degrade), walk-order sort untested (mutation survived), unpinned requirements.txt outside the key (→ requirements.txt joins the digest; ENGINE_VERSION-bump discipline documented).
section). PR split: adopted (3+1).

## Execution

_Filled in after implementation._

- **PR-a (engine identity + FC-092):** Status **Done** — merged as squash `fed6cec` 2026-09-01 (+ guard-fix PRs #111 and #113; deployed at `dbe78b5`). Confirmation pass: CONFIRMED-CLEAN. **Live verification:** 12/12 cells of run `b5acb93ad5b94f5e` stamped `5de01757715bc030`, byte-equal to the locally computed hash AND to the value `compute-engine-identity` baked into the dashboard image (build log confirmed); an identical resubmit answered DEDUPLICATED in 3 s wall. Deploying cost three test-only ambient-environment reds (#110 fastapi import, #111 the build's own workspace artifact, #113 no git binary in the slim image) — the class's sixth lifetime instance; CI-equivalent-venv pre-merge check adopted as guidance.
  **PR:** https://github.com/memon1987/agentic_options_wheel/pull/109 ·
  **Branch:** `fc-096/engine-identity` · **Date:** 2026-09-01.
  **Reviews:** concurrency/logging **APPROVE**; identity/dedup
  **REQUEST_CHANGES** → all five items addressed in code (below).
  **Landed:** `src/backtesting/scenarios/engine_identity.py` (new;
  `engine_identity()` = sha256[:16] over sorted relative paths + CONTENTS of
  every file under `src/`, all types, `ENGINE_VERSION` mixed in, cached per
  process, stdlib-only, `__main__` entry point) · `identity.sweep_key`
  `git_commit` → `engine_identity` (parameter, as before) · additive NULLABLE
  `engine_identity` on BOTH sweep tables, stamped by Job/CLI/dashboard, required
  by BOTH dedup predicates · `cloudbuild.yaml` `compute-engine-identity` step +
  `--build-arg` into `dashboard/Dockerfile` (`ARG`→`ENV`) · `services/sweeps.py`
  reads the env and disables the hint loudly (one log/process) when it is absent
  · FC-092 items 1 and 2 fixed (`utils.logger.tally_dispatch`, a process-stable
  processor reading the active tally from a `contextvars.ContextVar`;
  `summary()`/`binding_constraint()` sorted with an explicit tiebreak).
  **Notes / deviations from this plan's text:**
  1. **The cloudbuild step runs the module as a FILE, not as an import.** The
     plan's illustrative
     `python -c "from src.backtesting.scenarios.engine_identity import …"` does
     not work in the bare `python:3.11-slim` the step needs to be: that
     package's `__init__.py` imports the runner, so the import form drags in
     pandas and the Alpaca SDK. `python src/backtesting/scenarios/engine_identity.py`
     executes the same shared module with nothing but the standard library.
     Shared-module invocation (the property the plan actually requires) is
     preserved and pinned by `tests/test_cloudbuild_contract.py`.
  2. `build-dashboard-image` became a `bash -c` step (it has to read the value
     out of `/workspace`) and FAILS the build on an empty identity. Both the new
     step id and the new `waitFor` edge are re-frozen in
     `tests/fixtures/cloudbuild_contract.json` as its FIFTH deviation.
  3. `ENGINE_VERSION` is duplicated into `engine_identity.py` (the
     `services/sweeps.py` idiom) with a byte-equality pin, because `screen.py`
     imports the engine and this module must stay stdlib-only.
  4. Two prose sites that asserted the FC-092 defect as live were corrected in
     the same commit: `scenarios/report.TALLY_CAVEAT` (+ its pinned dashboard
     copy) and `docs/bigquery/backtest_runs.md`, which now records that
     `binding_constraint` is NULL by artifact on every pre-fix row and must be
     read as "not measured".
  5. **NOT done, deliberately:** `scenario_runs` still carries no
     `binding_constraint` column. The tally now works, but adding the column is
     a schema change with its own consumers and its own review, not a side
     effect of a logging fix. Worth a follow-up FC if Phase E's rejection panel
     wants it off the sweep rows rather than out of PR-b's detail artifact.
  **Fix round (round 1 → commit 2), from the identity/dedup REQUEST_CHANGES:**
  1. *(HIGH, split with the operator.)* The live tables had no
     `engine_identity` column, so a post-deploy submit would have 500'd on the
     hint query (`_sweep_query` re-raises a non-NotFound as a generic Exception)
     and, even under `force`, died at `insert_sweep_status`. The operator runs
     the two additive `ALTER TABLE … ADD COLUMN engine_identity STRING` as
     rollout step 0. The CODE half makes that state survivable anyway: the hint
     treats "Unrecognized name: engine_identity" as a **hint-miss** (one log,
     `sweep_dedup_hint_disabled`, returns None), and the insert **retries once
     without the field** (`engine_identity_column_missing`) — because
     `insert_rows_json` rejects the whole request over one unknown key, so the
     real choice is a stamp-less row or an invisible sweep. Both predicates are
     deliberately narrow: a 403, a 404 and a typo'd column name all still raise.
  2. *(MEDIUM.)* `_walk`'s final sort was load-bearing and untested — the
     reviewer deleted it and all 28 tests passed, because one filesystem returns
     one readdir order consistently. A harness that materialises the real walk
     and re-orders it (dirs, files, both) now pins digest invariance, plus a
     guard test that the harness really does reorder. **Mutation-checked:
     deleting the sort now fails 4.**
  3. *(MEDIUM.)* Dependency drift: `requirements.txt` is fully unpinned, so a
     deliberate dependency edit had stopped invalidating the key — a regression
     hidden inside an improvement, since `git_commit` used to catch it. The
     repo-root `requirements.txt` is now in the digest (`EXTRA_ROOT_FILES`,
     path-framed like every other entry; a missing one is refused, not ignored).
     Rebuild-resolution drift stays outside the key and is documented as a
     residual in the module docstring and `docs/bigquery/scenario_runs.md`, with
     the discipline that covers it: **bump `ENGINE_VERSION` on any dependency
     change that could move replay numerics.** Pinning the file is the real fix
     and is a separate change. **Mutation-checked: dropping the file fails 3.**
  4. *(LOW.)* A symlink anywhere under `src/**` (or at an extra file) is now
     refused with `EngineTreeError` — a followed link reads bytes from outside
     the boundary, and a linked directory can add an invisible subtree or a
     cycle. `find src -type l` is empty today, and a test states that
     precondition so a future commit fails here with an explanation.
  5. *(LOW.)* `RejectionTally.__enter__`'s swallow now logs
     `rejection_tally_bind_failed` — a tally that fails to bind counts nothing,
     and every consumer reads an empty tally as "never blocked", which is the
     FC-057 class arrived at by silence. `docs/bigquery/backtest_runs.md` now
     anchors the FC-092 boundary to **PR #109 / `f18d0c5`** as well as the date.
  *(Deferred to PR-c, where the plan already requires them: the executable
  thread-contract tests for the tally.)*

  **Suite:** 2871 passed after rebasing onto `abdef1e` (PR #108, the DTE knob);
  the merge-base collects 2793, so **78 new tests** — the same delta as before
  the rebase, which is the check that nothing of either side was lost in it.
  `tests/test_engine_identity.py` 38 (new file), `test_dashboard_sweeps.py` +22,
  `test_backtest_rejections.py` +10, `test_cloudbuild_contract.py` +6 (4 new
  cases plus 2 from the bash-validity parametrize, which now covers the two
  steps this PR makes `bash -c`), `test_scenario_persist.py` +2 net (two more
  were renamed off `git_commit`). Every behavioural claim in this PR is
  mutation-checked: reverting the tally binding fails 3, the tally ordering 2,
  the walk sort 4, the `requirements.txt` entry 3, the hint degrade 7, the
  insert retry 3.
- **PR-b (detail artifacts):** Status **Executing** (PR open).
  **PR:** https://github.com/memon1987/agentic_options_wheel/pull/112 ·
  **Branch:** `fc-096/detail-artifacts` · **Date:** 2026-09-01.
  **Reviews:** both **REQUEST_CHANGES** on the round-0 build (`293f7b9`,
  +107 tests) — `roll_records` fixture pinned a shape production never
  emits (-> re-pin to the real roller record + stamp the decision day);
  `artifacts_complete` vacuous/crashed semantics; the `$`-regex
  trailing-newline class; bucket-`NotFound` masking. All honesty invariants
  held under probing (bit-level row reconciliation, masked reach, fill
  stamp, sink ordering, traversal — zero GCS touches on hostile input).
  **Phase E data boundary (decided at this round): the artifact carries
  trades/curve/cycles/tally only; price series and B&H come from a bars
  path defined in Phase E's plan.**
  **Landed:** `src/backtesting/reporting/artifact.py` (new;
  `cell_artifact(result, ArtifactMeta) -> dict` — `daily` incl. `shares_held`,
  every `LedgerEvent` field with its free-form per-`kind` `detail`,
  `build_cycles` rows, `roll_records`, the dividend/early-assignment/unpriced
  counters, the ranked rejection tally, and the provenance block;
  `"schema": 1`) · `reporting/artifact_store.py` (new; deterministic gzip +
  best-effort GCS writer, lazy client, counts its failures, never raises) ·
  `scenarios/identity.py` gains `artifact_object_name` /
  `parse_artifact_object_name` **and the "no `__` in a scenario name" rule** —
  they live there because it is the module `dashboard/Dockerfile` copies flat,
  so writer and reader are literally one function · `runner._replay_one` gains
  `artifact_sink` (threaded through `run_sweep` with `run_id` /
  `engine_identity`), called LAST, in its own guard · `persist.py` additive
  NULLABLE BOOL `artifacts_complete` + `status_row` parameter · `main.py` wires
  the writer to the existing `persist` gate (Job always, CLI only under
  `--persist`) and stamps the terminal row via `_artifacts_complete` ·
  `dashboard/backend/services/artifacts.py` (new) +
  `GET /api/v2/sweeps/{run_id}/artifacts/{scenario}/{symbol}/{split}` +
  `google-cloud-storage==2.19.0` · docs.
  **Notes / deviations from this plan's text:**
  1. **The plan's premise that "the dashboard insert already degrades on
     unknown fields (PR-a)" was FALSE**, and the deviation is the fix. PR-a's
     predicate matches the literal string `engine_identity`, so a live table
     without `artifacts_complete` would have failed EVERY dashboard submit
     (`insert_rows_json` rejects the whole request over one unknown key) —
     reproducing PR-a's own outage verbatim. `sweeps.ADDITIVE_OPTIONAL_COLUMNS`
     is now a CLOSED SET of `(column, type)` pairs, with one retry per column
     and a per-column one-shot log; a typo'd column still raises.
     `mentions_missing_identity_column` is unchanged and still guards the hint
     query, which only references `engine_identity`.
  2. **The masked-reach stamp carries three numbers, not one:** the arm's
     `max_dte`, the `dte_cutoff` it implies (`+ UNIVERSE_DTE_BUFFER`, which is
     what a quote was actually filtered on), and `sweep_max_dte` as clearly
     labelled context. The plan's test wording ("stamps 7+buffer, not 22")
     reads as the cutoff and its §B2 wording reads as the reach; both are on
     the object under distinct names, so neither can be mistaken for the other.
  3. **Transport decision (the plan left it to the builder):** the object is
     stored as opaque `application/gzip` with NO `Content-Encoding` metadata —
     declaring it turns on GCS decompressive transcoding, so a download would
     silently return plain bytes and every reader's gzip handling would be a
     lie that happens to work — and the endpoint DECOMPRESSES SERVER-SIDE,
     returning `application/json`. Documented in `services/artifacts.py` and
     `docs/CLAUDE.md`.
  4. The GCS writer is a SIBLING (`artifact_store.py`) rather than inside
     `artifact.py` (the plan allowed either): the serialiser then stays pure and
     testable with no network, and the only code that touches GCS is twenty
     lines — `chain_store.py`'s split, for its reason.
  5. **NOT done, deliberately:** the live `ALTER TABLE
     options_wheel.scenario_sweeps ADD COLUMN artifacts_complete BOOL` — no GCP
     write access from the build context. It is rollout step **0b** for the
     coordinator; both belts hold if it lags (the Job's reconcile adds it, and
     the generalised dashboard degrade survives its absence loudly). The bucket
     IAM grant (rollout 2) is likewise the operator's.
  **Suite:** 2978 passed, from 2871 at the merge base — **+107**, exactly the
  two new files (`tests/test_sim_artifacts.py` 50,
  `tests/test_dashboard_artifacts.py` 57); no tests were added to existing
  files, which is the check that the delta is what it claims. Seven mutation
  checks, all caught: masked reach -> the sweep's; sink before the sensitivity
  pass; errored cells counted in `artifacts_complete`; the `__` rule removed;
  the degrade narrowed back to one column; `artifacts_complete` dropped from the
  API row shape; the sink's `try/except` removed. **No GCP writes** — every test
  injects a fake GCS client, and `test_dashboard_artifacts.py` carries an
  autouse guard that makes constructing a real `storage.Client` fail.
- **PR-c:** · **Commit:** · **Date:** · **Notes:**
- **PR-d:** · **Commit:** · **Date:** · **Notes:**
