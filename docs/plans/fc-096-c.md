# Plan: FC-096 Phase C — the covered-call selector: profile-aware replay on a synthetic lot

**FC entry:** `docs/FUTURE_CONSIDERATIONS.md` FC-096 (implements signed decision D2)
**Plan file:** `docs/plans/fc-096-c.md`
**Status:** Executing (PR #129 open at `cc9d7ae` 2026-09-09 — two adversarial reviews in flight; rev 3 + the appended preconditions and the 2026-09-08 operator decisions are the contract; §C3's "Roller: inert for CC" is SUPERSEDED — FC-100 wired rolling, the replay rolls at 1.00)
**Scope:** covered_call (primary) + shared (backtesting engine, sweep spec, sim service/Job/dashboard pass-through)
**Size:** L (two PRs)
**Author:** Claude (Fable), under the operator's standing authorization
**Last updated:** 2026-09-02 (rev 3)

---

## Context

Every replay to date simulates the WHEEL; the sweep spec has no strategy
dimension. D2 (operator-signed): covered-call sims run on a synthetic lot —
100 shares at the window-start price — symbol-comparable, candidate-friendly,
premise labelled everywhere. Round-1 reviews (2026-09-02, both
REQUEST_CHANGES) proved rev 1 under-specified the ENGINE MATH half: the CC
profile's spread gate total-rejects on model chains (10/10 probe), the
zero-cash lot books itself as profit in every stored equity return, the
verdict layer inverts for CC (never-called-away → "insufficient"), the roller
claim was false against the tree (→ FC-100 filed: the LIVE CC service does
not roll), profit-taking does not exist in the replay (52% of real calls
close early), `build_cycles` fragments on the seeding event, and one `Config`
conflated strategy-to-simulate with process storage context. Rev 2 resolves
all of it; §Review disposition maps each finding.

## Goals

1. Sweep spec gains `strategy: "wheel" | "covered_call"` — legacy identity
   byte-stable (omission canonicalisation), all four entry points resolve it.
2. A CC replay that measures what a continuing overwriting program earns on a
   held lot, with honest denominators, verdicts, and divergence footers.
3. Premise + strategy stamped on rows, artifacts, reports, keys.
4. Wheel behaviour byte-identical everywhere (golden-pinned).

## Non-goals

- No my-basis overlay; no CC-vs-wheel combined sweeps; no live-service
  changes (FC-100's roll decision is the operator's, separately); no lake
  work; no wheel-side monitor modelling (its own FC — it would change every
  stored wheel result).

## The strategy/context split (the rule everything below follows)

A spec's `strategy` selects the **strategy profile** — the `Config` the
REPLAY runs under (gates, knobs, legs). It never selects the **service
context** — dataset, buckets, credentials, provenance — which belongs to the
PROCESS. All CC measurement rows therefore land in the process's own store
(`options_wheel.scenario_*` for our CLI/Job/sim-service), self-described by
the `strategy` column. **FC-075 DD-4 exception, argued on the record:** the
dataset-isolation doctrine protects TRADING writers from cross-contamination;
scenario tables are measurement stores of hypotheses — one store with a
strategy column is the design (splitting it would orphan CC trend rows from
every reader). The four writer sites that must obey the split: `main.py`
`run_sweep_cmd` (CLI+Job), the battery's `writer_override`, and
`deploy/sim_service.py`'s writer — each keeps its process dataset while the
replay config swaps.

## Two PRs

- **PR-1 — engine + spec + service resolution**: identity, profile plumbing
  at ALL FOUR entry points (CLI, Job, battery, sim service — the sim-service
  resolution is IN PR-1; between-PR poisoned rows are worse than a bigger
  PR), synthetic lot, call-only replay, CC math/verdict, monitor leg (CC
  only), stamps/footers, `restrict_symbols` fix.
- **PR-2 — the mirrors**: dashboard validate_spec/allowlist/value-types +
  refusal parity, battery standing-set flip, docs.

## Proposed approach

### C1 — spec + identity (PR-1)

- `strategy` optional enum in `main.SPEC_FIELDS` and (PR-2) `validate_spec`;
  enum/case validation lives in the VALIDATORS — engine side: `load_spec_from_env` + the `SPEC_FIELDS` check and `scenarios_from_spec` in `main.py`; dashboard side:
  `validate_spec` — never in stdlib `identity.py`.
- **Canonicalisation by OMISSION**: absent OR explicit `"wheel"` → the field
  is omitted from the canonical dict; only `"covered_call"` is written.
  (Probe-verified: writing `"wheel"` in changes every legacy key; omission is
  byte-stable. Folding explicit-wheel matters because the dashboard's
  normalised spec may stamp it.) REQUIRED test: `canonical_spec` and
  `pin_identity` byte-stability for legacy specs — asserted on the canonical
  JSON, not on stored-row dedup (any `src/**` change moves engine identity
  anyway).
- Profile resolution per entry point: CLI — `--config` **default becomes
  None**; resolution order = spec's strategy → profile file, a SUPPLIED
  `--config` is an override, and a supplied config whose `strategy_id`
  contradicts the spec's strategy is refused loudly (three ambiguous
  implementations existed; this is the pinned one). **Two consequences pinned (confirmation-pass fixes):** (a) the PROCESS context — the pre-dispatch bootstrap at `main.py:80-88` (`Config(args.config)` → analytics writer), the battery's own config, and every writer dataset — resolves to `config/settings.yaml` (wheel) whenever `--config` is absent; only the REPLAY config follows the spec's strategy. (b) `run_sweep_cmd` keys the supplied-override refusal on **`args.config is not None`** (the CLI flag), NEVER on the positionally passed `Config` object — that object is process context, and reading it as an override would refuse every battery CC standing item (the deployed Jobs pass no `--config`). Job — same path
  (`run_sweep_cmd`), profile files ship in the image. Battery — resolution
  INSIDE `run_sweep_cmd` (the battery is the fourth entry point; its CC
  standing specs must not snapshot the wheel profile); the CC standing set
  iterates the WHEEL profile's live symbols (the CC profile deliberately has
  no `stocks:`). Sim service — `get_config()` becomes a per-strategy cached
  pair (both profiles loaded lazily once per process); per request, the
  spec's strategy selects the REPLAY config for `base_config_snapshot`/
  `base_config_hash`/`engine_config_hash`/`run_sweep`, while the writer
  dataset/artifact bucket stay process-context (the split above). The
  singleton docstring's "every request sees the same effective configuration"
  is rewritten to "…the same configuration PER STRATEGY".
- `base_config_hash` per profile = the dedup belt across strategies (test:
  wheel and CC runs of identical windows never dedup into each other).

### C2 — the synthetic lot (PR-1)

- `BacktestBroker.deposit_shares(...)` emitting
  `LedgerEvent(kind="synthetic_lot_open", cash_delta=0, detail={premise,
  shares, cost_basis})`. Zero cash delta stands, but the RETURN MATH is
  fixed with it (C3): the lot is an assumption, not income.
- `Simulator(synthetic_lots=...)`; seed on each symbol's first traded day at
  that day's close, before the scan. Cost-basis floor binds on the lot
  (verified reachable). `restrict_symbols` fixed to tolerate profiles with
  no `stocks:` section (rev-1 miss; in the files list).
- **Post-call-away posture — OPERATOR-GATED (D2 addendum, surfaced
  2026-09-02):** RECOMMENDED: re-seed at the next session's close (a
  continuing overwriting program; new `synthetic_lot_open`, new cycle,
  called-away counted per event; windows stay symbol-comparable).
  ALTERNATIVE: measurement stops at call-away (cash sits; coverage denominators
  truncate at the event). Both are specced; **PR-1's build does not start
  until the operator picks one.** Every metric below is defined to be
  well-formed under either.

### C3 — CC math, gates, and management fidelity (PR-1)

- **Spread gate**: on model-built chains `max_spread_pct: 0.10` rejects
  every premium-floor-clearing call by construction (probe: 10/10; model
  half-spread ≥ 5% of mark for OTM). The REPLAY suspends the spread gate
  (`max_spread_pct → None`) with (a) a `MODEL_SPREAD_BIAS` line in the CC
  footer stating the suspension and why, (b) a test pinning the suspension
  to the model hardcode (mirrors the FC-097 OI suspension: it fails loudly
  the day real spreads arrive so the gate is restored deliberately). Live
  divergence stamped, not hidden.
- **Return denominators**: `compute_fitness`/`SimulationResult` gain a
  strategy-aware capital base = `starting_cash + Σ seeded lot values` —
  the lot can never read as profit (REQUIRED test: a no-trade CC window
  returns 0.0%, not +lot%). The CC path pins `starting_cash` to a small
  stated buy-back float (`CC_CASH_FLOAT = 5_000`, documented: enough for
  BTC legs; keeps equity returns ≈ lot returns instead of diluted 20×).
  NEW row field `premium_yield_on_lot` (annualized net premium ÷ the
  TIME-WEIGHTED average seeded lot value — well-formed under both call-away postures: with a single lot it equals the lot value; under re-seed, each lot contributes value × days held ÷ window days) = the CC headline; `annualized_return` remains the corrected
  equity return; the CC footer + docs state which is which, and Phase E
  labels them (recorded in the FC entry's component note).
- **B&H**: CC branch constructs `BuyAndHold(shares=lot_shares,
  starting_cash=lot_value)` — the SAME lot, not a 100k-full-invest
  benchmark. `excess_return` is then honest.
- **Verdict (CC branch, cutoffs defined)**: `insufficient` = window <
  MIN_HOLDOUT-compatible length or chain coverage below the existing
  thresholds — NEVER "no closed cycle". Measured CC verdict from:
  `premium_yield_on_lot` vs the lot B&H's dividend-inclusive return
  (excess ≥ 0 → fit-leaning), coverage-by-reason, called-away terms
  (assignment P&L vs basis). `low_activity` for CC = writable-day coverage
  below 30% EXCLUDING hold-uncovered and earnings-span days (standing down
  below basis is the strategy working; the metric must not punish it).
- **Coverage, split by recorded reason** (the tally already labels them):
  covered / hold-uncovered (below basis) / earnings-span / gate-rejected /
  post-call-away. A single ratio is forbidden in the report; the split is
  the metric. Plus **net basis reduction** (cumulative net premium ÷ lot
  basis) — the number CC programs manage to.
- **Monitor leg (CC only)**: the replay adds `/monitor`-equivalent
  DTE-banded profit-taking into the day loop, gated to
  `strategy_id == "covered_call"`, using the CC profile's bands —
  `should_close_call_early` is deterministic config math over daily marks.
  Wheel replay untouched (golden). The wheel's own 52% early-close
  divergence keeps its existing footer line; a follow-up FC will own
  wheel-side modelling. NOTE (FC-086 carries it): the live bands are
  DTE-keyed ≤7 vs CC's 14-DTE target — the replay uses the profile's bands
  as configured and the footer names the gap.
- **Roller**: inert for CC — mirroring LIVE behaviour (FC-100: the live CC
  service has no rolling config; the operator decides wire-vs-declare).
  The plan tracks FC-100: if the operator wires CC rolling, a one-line
  profile change activates it here identically.
- **`build_cycles` IS touched** (rev-1's "untouched" struck): a
  `synthetic_lot_open` branch records shares/basis exactly as
  `put_assignment` does, so the cycle stays open, `cost_basis` is real,
  `call_assignment` books stock P&L, and re-seeding (if signed) opens a
  clean next cycle. Regression: wheel ledgers contain no such event —
  golden-pinned.
- **Splits**: `UnadjustedCorporateAction` refusal stands; the CC footer
  names the momentum-name selection bias it implies (12-month windows).
- **Stale wheel-calibrated bias copy**: the dividend/early-assignment and
  premium-parity lines in `SWEEP_BIASES` get strategy-conditional review in
  PR-1 (the 14-DTE payers may fire ex-div assignment for the first time).

### C4 — gates and refusal parity (PR-1 engine / PR-2 mirror)

- `_call_only_opportunities` moves to `src/strategy/strategy_gates.py`;
  `deploy/cloud_run_server.py` keeps a RE-EXPORT (existing tests patch
  `server.log_error_event` — patch targets updated where owned, re-export
  keeps external shape); the emitted event keeps
  `component="cloud_run_server"` VERBATIM (BQ views group by component; a
  test pins the string against well-meaning correction).
- **Strategy-conditional override refusal lives in `overrides.py`**
  (flat-copied — the only home all four entry points + the dashboard
  share): `validate_overrides(..., strategy=...)` refuses put-side keys for
  covered_call (and nothing new for wheel) with byte-identical reasons; the
  dashboard passes the spec's strategy; `OVERRIDE_VALUE_TYPES` unchanged.
  Both directions tested at both ends (engine + dashboard mirror) — a CC
  spec overriding `put_delta_range` is 422 at the dashboard AND refused by
  the Job, same words. `REJECTED_OVERRIDES["strategy_id"]`'s reason text
  updated (the "chosen by --config" claim becomes false).
- **Footer plumbing, signals named**: engine side conditions on a new
  `SweepResult.strategy` (threaded like `effective_max_dte`); dashboard
  side conditions on stored `spec_json.strategy` absent→wheel (the
  `spec_max_dte` pattern); `SYNTHETIC_LOT_BIAS` + `MODEL_SPREAD_BIAS`
  constants byte-pinned in `sweep_report_text.py` and the copied-constants
  test list.

## Critical files

PR-1: `engine/broker.py` (deposit + event) · `engine/simulator.py`
(synthetic_lots, seeding, monitor leg, `restrict_symbols` no-stocks fix) ·
`metrics/cycles.py` (`synthetic_lot_open` branch) · `metrics/fitness.py`
(capital base, CC verdict, lot B&H) · `scenarios/{identity,runner,report,
overrides}.py` (omission canonicalisation, `SweepResult.strategy`, footers,
strategy-conditional refusals) · `reporting/{artifact,report}.py` (stamps,
`premium_yield_on_lot`) · `persist.py` (new row field — additive) ·
`src/strategy/strategy_gates.py` (new) + `deploy/cloud_run_server.py`
(re-export) · `main.py` (`--config` default None, resolution order,
SPEC_FIELDS) · `deploy/sim_service.py` (per-strategy config pair — IN PR-1)
· fixtures (artifact per-kind set regenerated from a REAL CC replay).
PR-2: `services/sweeps.py` + `sweep_report_text.py` + allowlist ·
`main.py` (battery standing set) · docs (`BACKTEST_ENGINE.md`, `CLAUDE.md`,
`scenario_runs.md`, stale gate-home references).

## Reuse

The CC service's real gates/strategy code (never forked); the
`DTE_REACH_BIAS` conditional-footer mechanism; `deposit_shares` mirrors the
assignment path's lot bookkeeping.

## Behaviour contract

- Wheel: golden sweep byte-identical; legacy `canonical_spec`/`pin_identity`
  byte-stable; no wheel ledger event/verdict changes.
- CC: zero put events; first per-symbol event `synthetic_lot_open`; a
  no-trade window returns 0.0%; wheel/CC never cross-dedup; CC rows land in
  the process's own dataset; footers present iff CC.
- The moved gate leaves the CC service byte-identical incl. the component
  string.
- The sim service resolves CC specs correctly from PR-1's merge (no
  poisoned-row window).

## Test requirements

Legacy-identity (canonical JSON byte-stability); golden wheel sweep;
no-trade-CC 0.0% (the lot-as-profit regression); 10/10-spread-probe pinned
as the suspension's justification + restore-on-real-spreads tripwire;
verdict branch cases (never-called-away ≠ insufficient; low-activity
exclusions); coverage-by-reason splits; lot B&H construction; monitor-leg
closes at band targets (CC) + wheel-untouched golden; cycles branch (open
cycle, real basis, assignment P&L, wheel regression); both-direction
override-refusal parity; four-entry-point profile resolution (incl. battery
CC standing spec snapshotting the CC profile; sim service per-strategy
hashes); `--config` matrix (bare/supplied×wheel/CC); re-export + component
string + patched-target tests; artifact fixture from a real CC replay; both
venvs; ordered pairs; CI venv rebuilt from requirements.

## Design decisions (made — do not relitigate in the build)

- Strategy/context split (above); CC rows in the process store — the DD-4
  exception argued on the record.
- Omission canonicalisation; enum validation in validators only.
- Zero-cash lot + strategy-aware capital base + `CC_CASH_FLOAT = 5000` +
  `premium_yield_on_lot` as the CC headline.
- Spread gate suspended on model chains with tripwire + footer.
- Monitor leg CC-only; roller mirrors live (FC-100 tracks the decision).
- `build_cycles` gains the seeding branch (rev-1's "untouched" was wrong).
- Sim-service resolution ships in PR-1.
- 100 shares fixed; no shares knob without operator sign-off.
- One strategy per sweep.

## Open questions

- **BLOCKING (operator, D2 addendum):** post-call-away — re-seed at next
  close (recommended) vs stop measuring. Surfaced 2026-09-02; PR-1's build
  waits on it.
- **Non-blocking:** FC-100's live-roll decision (the replay mirrors either
  outcome); wheel-side monitor modelling (follow-up FC).

## Rollout

1. Operator answers the re-seed question → PR-1 builds.
2. Merge PR-1. Verify: CLI CC sweep on 2 widened symbols — lot-first ledger,
   zero puts, coverage-by-reason populated, both footers, 0.0% no-trade
   guard; golden wheel sweep unchanged; a CC spec through the sim service
   (202 → done, correct hashes/dataset). NOTE: moving the gate into
   `src/strategy/` moves engine identity — stored sweeps recompute once;
   the first post-merge Saturday is the expensive one.
3. Merge PR-2. Verify dashboard refusal parity + allowlist text; battery CC
   standing rows the following Saturday (F/PFE/KMI/VZ will sit permanently
   insufficient at the premium floor — expected, in the sizing note).
4. FC-096 entry: Phase C shipped; Phase D (IAP — operator step) next.

## Review disposition (round 1 → rev 2)

Trader review: B1 spread-gate → C3 suspension+tripwire+footer; B2
denominators → C3 capital base/CC_CASH_FLOAT/premium_yield_on_lot; B3
verdict → C3 cutoffs + insufficiency redefinition; H4 roller → FC-100 filed
(live drift confirmed by probe: no env, no scheduler, no config), replay
mirrors live; H5 profit-taking → C3 monitor leg (CC-only) + footer; H6
restrict_symbols → C2/files; M7 B&H → C3 lot-sized; M8 call-away → C2
operator-gated with both postures specced; M9 coverage → C3
split-by-reason + basis reduction; M10 splits → C3 footer; L11 stale copy →
C3 conditional review; L12 battery arithmetic → rollout note.
Integration review: B1 dataset routing → the strategy/context split section
+ four writer sites; B2 canonicalisation → omission + validator-homed enum +
byte-stability test respec; H3 sim-service singleton → per-strategy cached
pair, split-aware; H4 inter-PR window → sim-service resolution in PR-1; H5
build_cycles → branch specced, "untouched" struck; H6 --config → default
None + pinned resolution order; M7 refusal home → overrides.py
strategy-conditional with dashboard parity; M8 move mechanics → re-export +
component string pinned + patch targets; M9 battery entry point → named,
resolution in run_sweep_cmd, wheel-universe iteration stated; M10 footer
signals → both named with the absent-default agreement; LOWs → reason text,
identity-move rollout note, doc refs in PR-2. Confirmation (rev 2 → rev 3): time-weighted lot-value denominator for the headline yield; process-context-resolves-to-wheel when `--config` absent; supplied-override keyed on `args.config is not None`; the nonexistent `normalise_spec` name replaced with the real engine-side sites.

## Execution

_Filled in after implementation._

- **PR-1:** · **Commit:** · **Date:** · **Notes:**
- **PR-2:** · **Commit:** · **Date:** · **Notes:**

## Precondition added 2026-09-03 (from the Phase E PR-2 review)

`src/backtesting/reporting/artifact.py` (Phase E PR-1) stamps `provenance.capital_base = starting_cash` whenever `meta.capital_base is None`, and the console trusts any stamped positive number as authoritative. A covered-call replay that forgets to set `meta.capital_base` therefore ships `capital_base = CC_CASH_FLOAT` ($5,000) and every console ratio divides by the float — exactly what §D-4 of the Phase E plan forbids, with the console's suppression path unreachable. **Phase C MUST (a) stamp `meta.capital_base = lot value` on every CC cell and bars sidecar, and (b) change the writer's default to REFUSE (stamp `null`) for any non-wheel `strategy` rather than fall back to `starting_cash`, with a test that a `covered_call` artifact without an explicit base carries `capital_base: null`.** Build gate unchanged (operator answers on post-call-away and FC-100).

## Precondition added 2026-09-04 (from the Phase E PR-5 review)

The compare view's alignment matrix has no STRATEGY row: a `covered_call` cell vs a wheel cell of the same symbol passes every row, and a CC artifact without `capital_base` falls back to the spec's `starting_cash` and can read "aligned" at $100k. **Phase C MUST add a strategy row (refuse cross-strategy pairs) alongside the `capital_base` stamp before any CC cell can enter `/sims/compare`.**

## Operator decisions signed 2026-09-08 (lift the build gate)

- **§C2 post-call-away posture: RE-SEED AT THE NEXT CLOSE.** When the synthetic lot is called away, the sim buys a fresh 100-share lot at the next session's close and keeps writing; every symbol is measured over the full window. Consequence the report must label: the stock leg is a CHAIN of lots (each with its own basis), and the `synthetic_lot_open` event recurs — the cycle builder and the console's cycle table/ledger show each lot; `premium_yield_on_lot` is over the time-weighted lot value across the chain. Rejected: stop measuring on call-away (biases against the names called away earliest).
- **FC-100: WIRE ROLLING for the covered-call service** (its own plan + PR, `docs/plans/fc-100.md`, BEFORE the Phase C build): a `rolling` block in `config/covered_call.yaml` (seven keys identical to the wheel's, `itm_trigger_ratio: 1.00` — true-ITM-only, a stated Symmetry difference), NO env change (the wheel carries no `ROLLER_ENABLED` either: yaml enables, env kills — FC-100 DD-2), and a `cc-roll-daily` scheduler (operator step, created paused, resumed only after FC-107 is live) mirroring the wheel's daily credit-only roller; Phase C's CC replay then models rolls with the same `CallRoller` gates. Rejected: declare no-roll deliberate.

Build order: FC-100 PR → Phase C build (this plan, Approved rev 3 + the two Phase E preconditions above + these decisions).

## Amendment (2026-09-09, from the Phase C build — binding on the review/fix rounds)

- **Two lot aggregations, stated:** `premium_yield_on_lot`'s NUMERATOR uses Σ seeded lot values (rev 3's formula); its DENOMINATOR is the TIME-WEIGHTED lot value across the re-seed chain (the 2026-09-08 decision). Using one for both leaves a non-zero `reconciliation_gap` on any chain; they coincide only for a single lot. The report states both definitions.
- **Fill model conflict → FC-116.** The FC-100 hand-off asked the replay to fill rolls at the placed limits; the engine has one fill model (haircut from mid) and changing it on the roll path breaks the wheel golden byte-identical contract. Phase C keeps haircut fills and the footer names BOTH roll biases with direction: credits biased UP (haircut fills) and counts/credits biased DOWN (22-DTE lake vs a 28-DTE need). FC-112's wheel trigger study waits for FC-116.
- **One PR, not two:** the 2026-09-04 strategy-row precondition crosses rev 3's PR split, so the build delivered PR-1 + PR-2 in one PR (#129).
- `MIN_COVERED_FRACTION` is a new engine constant (name it in the config census); the battery stays wheel-only by choice (a CC standing set is a later decision).

## Amendment 2 (2026-09-09, from the PR #129 reviews — binding on the fix round)

- **BLOCKER found:** the sweep path's `config_hash` reads `put_target_dte`, absent on the CC profile — every CC sweep entry point crashed; the plan's test list never drove `run_sweep` on `config/covered_call.yaml` (lesson: every "entry point resolution" test must reach the runner, not stop at `Simulator`).
- **One denominator (DECIDED):** `capital_base = lot_capital` (time-weighted across the chain); `CC_CASH_FLOAT` is a documented liquidity reserve EXCLUDED from every ratio; the benchmark's `starting_cash` = the same `capital_base`. Rev 3's `starting_cash + Σ lot` diluted `excess_return` by lot/(lot+5,000) and could flip its sign.
- **CC verdict WARN (DECIDED, corrects §C3):** WARN when the lot's TOTAL return < the lot B&H's total return; `premium_yield_on_lot` is a yield statement beside the dividend yield, never the WARN driver. §C3's premium-vs-total comparison called every rising never-assigned name "worse than holding".
- **Roll reach (DECIDED):** CC materialisation reach = `call_target_dte + rolling.max_extension_days` (28) capped at `MAX_SWEEPABLE_DTE = 21` (the lake's `universe_dte` 22 = 21 + the one-day universe buffer; consistent with every other DTE bound); ROLL_REACH_BIAS states the actual reach and that extensions truncate to ≤ (reach − k), plus the haircut-fill UP bias; `call_roll_skipped` reason counts surfaced. "PARTIAL, only at the ceiling" was wrong: the sweep's own 15-DTE reach bounded every evaluation.
- Compare view: `toSide()` must pass the spec strategy (the shipped page ignored it). `shape_results` parity on CC runs (`spec_max_dte` strategy-aware). Wheel-calibrated SWEEP_BIASES lines strategy-conditional. Artifact `counters.total_return` / `candidate_days` corrected for CC. `hold_uncovered` derived from the day's rejection, not close < basis. CC benchmark shares = `SYNTHETIC_LOT_SHARES`. Docs (`CLAUDE.md`, `BACKTEST_ENGINE.md`, `scenario_runs.md`) are part of the PR.
- **Re-seed critique, on the record (posture unchanged — operator-signed):** every re-seed is a forced buy at the post-call-away close, typically above the surrendered strike; the chain is momentum-following and the TW basis resets upward. `SYNTHETIC_LOT_BIAS` says so. Stop-measuring was rejected for the opposite bias; both are now named.

## Amendment 3 (2026-09-09, from the PR #129 confirmation — binding on the follow-up)

- `hold_uncovered` means: in-band strikes EXIST and ALL sit below basis. An empty delta band or a missing chain snapshot is `gate_rejected` (counted in the coverage denominator), never `hold_uncovered` — the first fix filed both as the floor's doing and flattered coverage on a stock far above basis. The footer's "errs toward `gate_rejected`, never flattering" holds only with that rule, and the test pins the classifier on an above-basis book with an empty band.
- The wheel sweep MARKDOWN is byte-identical too (separators guarded on an empty covered-call table), not only the numbers.
